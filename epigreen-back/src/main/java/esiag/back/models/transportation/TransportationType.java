package esiag.back.models.transportation;

public enum TransportationType {
    //A compléter avec plus de types / des types plus pertinents
    //Actuellement juste pour PoC
    TRANSPORT_EN_CAMION, RIGIDE, UTILITAIRE;
}
